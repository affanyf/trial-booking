// Re-exported from src/lib/internal-api.ts so the UI (Server
// Actions/Components) and this test suite share exactly one place that
// knows how to call our own Route Handlers in-process.
export * from "@/lib/internal-api";
